(()=>{const token=localStorage.getItem('token');let user=null;try{user=JSON.parse(localStorage.getItem('user')||'null')}catch(_){}if(!token||user?.role!=='Domiciliario'){location.replace('/login');return;}const $=id=>document.getElementById(id);const money=v=>new Intl.NumberFormat('es-CO',{style:'currency',currency:'COP',maximumFractionDigits:0}).format(Number(v||0));const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));async function api(url,opt={}){const r=await fetch(url,{...opt,headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`,...(opt.headers||{})}});const t=await r.text();let d;try{d=JSON.parse(t)}catch{throw Error(`HTTP ${r.status}`)}if(!r.ok)throw Error(d.detail||'Error');return d;}function card(o,active=false){return `<article class="delivery-card"><div class="top"><b>#${esc(o.short_id)}</b><strong>${money(o.total)}</strong></div><p>👤 ${esc(o.customer?.name||'Cliente')} · ${esc(o.customer?.phone||'')}</p><p class="address">📍 ${esc(o.address||'Sin dirección')}</p><p>🍳 Cocina: <b>${o.prep_seconds!=null?Math.round(o.prep_seconds/60)+' min':'calculando'}</b></p>${o.dispatched_at?`<p>🛵 En ruta: <b>${Math.max(0,Math.floor((Date.now()-new Date(o.dispatched_at))/60000))} min</b></p>`:''}${active?`<div class="actions"><button data-deliver="${o.id}">🔐 Confirmar entrega</button><button data-cancel="${o.id}" class="danger">Cancelar orden</button></div>`:`<button data-claim="${o.id}">🚚 Tomar pedido</button>`}</article>`}async function load(){try{const [a,m]=await Promise.all([api('/api/delivery/available-orders'),api('/api/delivery/my-orders')]);$('available').innerHTML=a.length?a.map(o=>card(o)).join(''):'<p>No hay pedidos listos esperando repartidor.</p>';const active=m.find(o=>o.status==='OUT_FOR_DELIVERY');$('active').innerHTML=active?card(active,true):'<p>No tienes una entrega activa.</p>';$('messages').innerHTML=(await api('/api/delivery/messages')).map(x=>`<div><b>${esc(x.sender)}</b><small>${new Date(x.created_at).toLocaleTimeString('es-CO')}</small><p>${esc(x.message)}</p></div>`).join('')||'<p>No hay mensajes.</p>';}catch(e){console.error(e)}}document.addEventListener('click',async e=>{const claim=e.target.closest('[data-claim]');if(claim){try{await api(`/api/delivery/orders/${claim.dataset.claim}/claim`,{method:'PATCH'});await load()}catch(x){alert(x.message)}}const del=e.target.closest('[data-deliver]');if(del){const code=prompt('Ingresa el código de 6 dígitos del cliente:');if(!code)return;try{await api(`/api/delivery/orders/${del.dataset.deliver}/delivered`,{method:'POST',body:JSON.stringify({code})});alert('Entrega confirmada. Caja debe registrar el pago y cerrar el pedido.');await load()}catch(x){alert(x.message)}}const cancel=e.target.closest('[data-cancel]');if(cancel){const reason=prompt('Motivo de cancelación:');if(!reason)return;try{await api(`/api/delivery/orders/${cancel.dataset.cancel}/cancel`,{method:'PATCH',body:JSON.stringify({reason})});await load()}catch(x){alert(x.message)}}});$('messageForm').addEventListener('submit',async e=>{e.preventDefault();const v=$('messageInput').value.trim();if(!v)return;try{await api('/api/delivery/messages',{method:'POST',body:JSON.stringify({message:v})});$('messageInput').value='';await load()}catch(x){alert(x.message)}});$('logout').onclick=()=>{localStorage.removeItem('token');localStorage.removeItem('user');location.replace('/login')};function track(){if(!navigator.geolocation){$('locationState').textContent='⚠️ Este navegador no permite ubicación.';return;}navigator.geolocation.watchPosition(async p=>{try{await api('/api/delivery/location',{method:'POST',body:JSON.stringify({latitude:p.coords.latitude,longitude:p.coords.longitude,accuracy:p.coords.accuracy})});$('locationState').textContent=`📍 Ubicación activa · precisión aprox. ${Math.round(p.coords.accuracy)} m`;}catch(e){console.error(e)}},e=>{$('locationState').textContent='⚠️ Debes permitir la ubicación para que Caja pueda ver al domiciliario.'},{enableHighAccuracy:true,maximumAge:10000,timeout:15000});}track();load();setInterval(load,5000)})();
