ENV=dev
# Para desarrollo rapido sin Docker, deja la linea de sqlite.
# Para usar Postgres (docker-compose up -d postgres), comenta la de sqlite y descomenta la otra.
DATABASE_URL=sqlite:///./criptonix.db
# DATABASE_URL=postgresql://criptonix:criptonix@localhost:5432/criptonix_erp
SECRET_KEY=cambia-esto-en-produccion
